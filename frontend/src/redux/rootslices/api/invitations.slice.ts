import {Result} from "../../../types/result";
import {baseApiSlice} from "./base.slice";

export const invitationsSlice = baseApiSlice.injectEndpoints({
    endpoints: (builder) => ({

    }),
});

export const {   } = invitationsSlice;
